{
  "name": "interview-os-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root configuration for Interview-OS monorepo deployment",
  "scripts": {
    "build": "npm run build --prefix frontend"
  }
}
